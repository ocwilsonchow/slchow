import { Spinner } from "@slchow/ds/components/ui/spinner"

const Loading = () => {
  return (
    <main className="h-screen grid place-items-center flex-1 shrink-0">
      <Spinner />
    </main>
  )
}

export default Loading
