---
title: Local Storage、Session Storage 與 Cookies
description: 實務指南：browser storage、其取捨，以及 production 中真正重要的安全邊界
category: frontend
---

Browser storage 看起來簡單，直到它成為 authentication flow、multi-tab 體驗，或必須撐過 schema changes 的應用的一部分。

<br />

- API 很少是困難之處。真正的決策是 **誰需要這些資料、它應該存活多久，以及它跨越了哪條安全邊界**。
- 瀏覽器並沒有名為 `cookieStorage` 的標準 API。傳統 cookies 走 `document.cookie`；較新的非同步 **Cookie Store API** 走 `cookieStore`。它們管理的是同一層底層 cookies，兩者的行為都不像 Web Storage。

<br />

## 概覽

| Property | `localStorage` | `sessionStorage` | Cookies |
| --- | --- | --- | --- |
| Typical lifetime | Until explicitly cleared or evicted | Current tab's page session | Session-based or until their expiry date |
| Sent to server | No | No | Yes, on matching requests |
| JavaScript access | Yes, synchronously | Yes, synchronously | Yes, unless marked `HttpOnly` |
| Best suited for | Persistent, non-sensitive preferences | Temporary state that should survive reloads | Server-managed sessions and small server-readable values |

<br />

- Web Storage 屬於 **client**。Cookies 參與 HTTP request lifecycle。
- 誰都不該被當成受信任的 database。

<br />

---

## localStorage

`localStorage` 把資料為某個 origin 持久保存，撐過 page reloads 與 browser restarts。

<br />

```ts
const preferences = {
  theme: "dark",
  density: "compact",
}

localStorage.setItem("preferences:v1", JSON.stringify(preferences))

const stored = localStorage.getItem("preferences:v1")
const parsed = stored ? JSON.parse(stored) : null
```

<br />

```ts
type Preferences = {
  theme: "light" | "dark"
}

function readPreferences(): Preferences | null {
  try {
    const value = JSON.parse(localStorage.getItem("preferences:v1") ?? "null")

    if (value?.theme === "light" || value?.theme === "dark") {
      return value
    }
  } catch {
    // Corrupt or manually edited storage should not break the application.
  }

  return null
}
```

<br />

- 適合少量、非敏感 preferences：theme、已關閉的 notice、未完成的 local draft。
- Values 是 **strings**，訪問是 **synchronous**，每一次 read 或 write 都會擋住 main thread。Capacity 與 eviction 因 browser 而異。它是小型 persistence 機制，不是 database。
- Version keys，並在讀取時 **validate**：已部署的應用會變，舊的 browser 資料還在。
- **Failure：** 把 session tokens、passwords，或長期 bearer tokens 放進 `localStorage`。該 origin 上的任何 script 都能讀它，包括 XSS。偷走這個值，就足以在別處重用。

<br />

---

## sessionStorage

`sessionStorage` 有類似的 string-based API，但它的 lifetime 綁在 **page session**。

<br />

```ts
sessionStorage.setItem(
  "checkout:draft",
  JSON.stringify({ step: 2, deliveryMethod: "pickup" })
)
```

<br />

- 它能撐過同一 tab 裡的 reloads，通常在該 tab 或 window 關閉時被清除。分開的 tabs 得到分開的 storage。
- 適合臨時 UI state：multi-step form、return URL，或應撐過一次意外 refresh、卻不該變成永久 preference 的 filters。
- **Failure：** 把 `sessionStorage` 當成安全邊界。頁面上的 scripts 仍然能讀它，用戶也可以用複製或恢復 tabs 的方式，讓 lifetime 假設變得沒那麼明顯。它適合的是 **product lifetime** 與資料匹配的時候，而不是因為它比 `localStorage` 更安全。

<br />

---

## Cookies 與 Cookie Store API

Cookies 不同，因為 browser 可以把它們附在 **HTTP requests** 上。

<br />

```http
Set-Cookie: session=opaque-value; Path=/; HttpOnly; Secure; SameSite=Lax
```

<br />

```ts
const preference = await cookieStore.get("theme")

await cookieStore.set({
  name: "theme",
  value: "dark",
  path: "/",
  sameSite: "lax",
})
```

<br />

- 適合 **server** 需要這個值的時候，尤其是 server-managed sessions。Security-sensitive cookies 通常應由 server 創建。
- `HttpOnly` 阻止 JavaScript 讀取 cookie，`Secure` 把它限制在 HTTPS，`SameSite` 有助於限制 CSRF。它們降低風險；它們不能取代 output escaping、CSP、CSRF analysis，或 session expiration 與 rotation。
- 傳統 `document.cookie` 是 synchronous 的，解析也很彆扭。**Cookie Store API** 在支持的地方是 asynchronous。Client-side code 仍然無法透過 `cookieStore` 讀取 `HttpOnly` cookie——這正是限制的意義。
- **Failure：** 因為 `cookieStore` 看起來像 `localStorage`，就把 preferences 塞進 cookies。Cookies 很小，domain 與 path 規則很微妙，而且隨 requests 帶上的 cookies 每次都會增加網絡開銷。

<br />

---

## 預設選擇

以最短的有用 lifetime，存放最少的資料。

<br />

- 當資料只需在當前頁面打開期間存活時，使用 **in-memory state**。
- 當臨時 state 應在單一 tab 中撐過 reloads 時，使用 **sessionStorage**。
- 對應需要跨多次訪問持久保存的少量、非敏感 preferences，使用 **localStorage**。
- 當 server 必須收到該值時使用 **cookies**，尤其是放在 secure、HTTP-only cookie 中的 opaque session identifier。
- 當應用需要大量資料、結構化 records、transactions，或 offline 行為時，改用 **IndexedDB**。
- Browser storage 由用戶控制，可被清除或修改，絕不應被當成權威的 source of truth。讀取時驗證、處理 storage failures，並讓 server-side authorization 獨立於 client 可編輯的任何內容。

<br />

選擇 storage 是架構決策，而不是 API 偏好。從資料的 lifetime 與信任等級出發；正確的 browser primitive 通常會隨之而來。

---

## Recap Q&A

<Accordion type="single" collapsible>
  <AccordionItem value="storage-who-how-long">
    <AccordionTrigger>1. localStorage、sessionStorage 與 cookies 實際差在哪裡？</AccordionTrigger>
    <AccordionContent>

      - API 很少是困難之處。真正的決策是 **誰需要這些資料、它應該存活多久，以及它跨越了哪條安全邊界**。
      - `localStorage` 為某個 origin 持久保存，直到被清除。`sessionStorage` 是 **page session**：撐過單一 tab 裡的 reloads；分開的 tabs 得到分開的 storage。兩者都不會送到 server。
      - Cookies **會**送，在匹配的 requests 上。Web Storage 屬於 client。Cookies 參與 HTTP request lifecycle。
      - 誰都不該被當成受信任的 database。
      - 沒有標準的 `cookieStorage`；傳統 `document.cookie` 與 **Cookie Store API** 管理同一層 cookies，兩者的行為都不像 Web Storage。

    </AccordionContent>
  </AccordionItem>

  <AccordionItem value="storage-httponly">
    <AccordionTrigger>2. 什麼時候該用 HttpOnly cookie 而不是 Web Storage？</AccordionTrigger>
    <AccordionContent>

      - 當 **server** 需要這個值時，cookies 才合適，尤其是 server-managed sessions。
      - Security-sensitive cookies 通常應由 server 創建：`HttpOnly; Secure; SameSite=Lax`。
      - **`HttpOnly`** 阻止 JavaScript 讀取 cookie。`Secure` 把它限制在 HTTPS。`SameSite` 有助於限制 CSRF。它們降低風險；它們不能取代 escaping、CSP、CSRF analysis，或 session rotation。
      - Session tokens、passwords 與長期 bearer tokens 不屬於 `localStorage`。該 origin 上的任何 script 都能讀它，包括 XSS。
      - Client-side code 仍然無法透過 `cookieStore` 讀取 `HttpOnly` cookie。這正是限制的意義。

    </AccordionContent>
  </AccordionItem>

  <AccordionItem value="storage-cookie-store">
    <AccordionTrigger>3. Cookie Store API 改了什麼，又沒改什麼？</AccordionTrigger>
    <AccordionContent>

      - 傳統 `document.cookie` 是 synchronous 的，解析也很彆扭。**Cookie Store API** 在支持的地方是 asynchronous：`cookieStore.get` / `set`。
      - 它是通往 **同一份** cookie jar 的更好 interface，不是新的 persistence primitive。
      - 一般 application state 仍然不屬於 cookies：它們很小，domain 與 path 規則很微妙，而且隨 requests 帶上的 cookies 每次都會增加網絡開銷。
      - Client-only state 優先用 Web Storage 或 memory。
      - **Failure：** 因為 `cookieStore` 看起來像 `localStorage`，就把 preferences 塞進 cookies。

    </AccordionContent>
  </AccordionItem>

  <AccordionItem value="storage-xss">
    <AccordionTrigger>4. 為什麼 sessionStorage 不是安全邊界？</AccordionTrigger>
    <AccordionContent>

      - 頁面上的 scripts 仍然能讀 `sessionStorage`。用戶也可以用複製或恢復 tabs 的方式，讓 lifetime 假設變得沒那麼明顯。
      - 它適合的是 **product lifetime** 與資料匹配的時候——checkout draft、return URL——而不是因為它比 `localStorage` 更安全。
      - 該 origin 上的 XSS 能讀 JavaScript 能讀的任何東西。
      - Version keys，並在讀取時 validate：已部署的應用會變，舊的 browser 資料還在。Corrupt 或被手動改過的 storage 不該弄壞 app。
      - 以最短的有用 lifetime 存放最少的資料，並讓 server authorization 獨立於 client 可編輯的任何內容。

    </AccordionContent>
  </AccordionItem>

  <AccordionItem value="storage-defaults">
    <AccordionTrigger>5. Browser storage 的預設選擇是什麼？</AccordionTrigger>
    <AccordionContent>

      - 資料只需當前頁面時，用 **in-memory**。臨時 state 應在單一 tab 中撐過 reloads 時，用 **sessionStorage**。
      - 跨多次訪問的少量、非敏感 preferences，用 **localStorage**。
      - 當 server 必須收到該值時用 **cookies**——通常是放在 secure、HTTP-only cookie 中的 opaque session id。
      - 需要大量資料、結構化 records、transactions，或 offline 行為時，用 **IndexedDB**。
      - Capacity 與 eviction 因 browser 而異。Web Storage 是 strings、synchronous，並且擋住 main thread。從 lifetime 與信任等級出發；primitive 通常會隨之而來。

    </AccordionContent>
  </AccordionItem>
</Accordion>
