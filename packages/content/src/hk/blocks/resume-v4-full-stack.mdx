---
title: 履歷 — 全端
---

## Wilson, Chow Sin Long

擁有 **5 年**經驗的全端 **software engineer**，在多客戶環境中交付正式上線應用——從 Figma 到 React／React Native、Node.js、型別安全 API 與 AWS。從零開始並多年維護 **15+** 個應用，包括 App Store／Play Store 上架。載入時間最多縮短 **80%**，搜尋延遲降低 **90%**。目前主導一個具備持久化 workflows、memory、RAG 與 evals 的多渠道 **Generative AI** agent 平台。已取得 **AWS Solutions Architect Associate** 認證。

---

## 工作經驗

### 軟件開發者，Vioo Company Limited，2022 年 5 月至今

擔任多個並行客戶產品的主要工程師——與技術和非技術持份者一起界定 UX，再從設計交付到雲端部署。

- 從零開始交付並多年維護 **15+** 個正式上線的 React／React Native 應用，包括高保真 Figma 設計、共享設計系統，以及 App Store／Play Store 上架。
- 以自研 headless CMS（Strapi／Sanity）與搜尋（Meilisearch／Algolia）取代 WordPress，置於 **Turborepo** 中並以 **SST** 部署，過渡期間發布流程不中斷：載入時間縮短 **80%**、搜尋延遲降低 **90%**，PageSpeed Insights **>90**。
- 在 Lambda／API Gateway 與 **ECS Fargate** 上建構型別安全 Node.js API（Hono、OpenAPI、tRPC）：用 Drizzle／Prisma 做按組織隔離的 Postgres，Better Auth、OAuth 2.0／OIDC，以及落在資料模型中的 RBAC。
- 建立共享的 Radix／Tailwind 元件與 hook 函式庫，並透過 Storybook、Fumadocs 和 MDX 撰寫文件，供多個客戶應用共用。為關鍵流程加入 Vitest／Cypress。
- GitHub Actions + SST 做 CI／CD——部署前跑 lint、typecheck、tests 與 build——部署時間縮短 **50%+**。
- 透過結對編程、程式碼審查與共同交付正式上線工作，指導暑期實習生與初級開發者。

<br />

---

## 精選專案

### 多渠道 Agent 平台，2026 年 4 月至今

面向客戶的持久化多渠道助手平台（Web + Discord）。

- 每次 agent run 是跨越 Web 與 Discord 的持久化串流：客戶端可斷線、重連並在執行途中恢復，而不會遺失進度。
- 基於 Mastra 與 AI SDK 建構 workflows、memory、schedules 與 tools：pause／resume、retries、沙盒工具與 skills。
- 端到端交付 RAG（ingest、chunk、Pinecone query／rerank），並以 evals 作為發布門控。
- 將 Hono + Better Auth 運行於 **ECS Fargate**。Postgres 存核心資料、S3 存檔案、ClickHouse 記 run／tool traces 與成本／延遲——緩慢的 trace 寫入不會卡住一次 agent run。
- 交付聊天、工具調用與多步客戶端流程，以及 TipTap @mention 檔案標記元件，用於將 workspace 檔案附加至 prompts。

---

### 高效能行銷網站，2025 年 11 月

擔任內容與搜尋型行銷網站的主要工程師——效能、CMS 與發布流程。

- Next.js + Strapi + Meilisearch，達到 **LCP &lt; 0.5s**、**CLS &lt; 0.1**，PageSpeed Insights **>90**。
- 混合 ISR：主要頁面與最新 100 篇文章在建置時生成，其餘按需；透過 Strapi webhook revalidation，發布後無需完整重建即可上線。
- 將無障礙與 SEO 納入交付標準；使用 AI SDK 建構用於生成式 AI 內容創作的自訂 Strapi plugin。
- 以 TanStack Query prefetch／hydration、TanStack Form + Zod 與 XState 建構互動與多步流程；客戶內容站點支援 locale-aware 路由、文案與日期／數字格式。

---

### React Native 會員平台，2024 年 4 月上線

- 主導 Figma 設計與會員應用；將 iOS 和 Android 發布至 App Store 與 Play Store。同時維護 Express API 與 Next.js 管理後台。
- 透過 Sentry 獨立診斷生產崩潰、裝置相容性與效能瓶頸，覆蓋 app 與 API；上線後維持 **99%** crash-free sessions。
- 使用 FlashList、tab freeze 與快取 feed 圖片改善列表卡頓。Firebase Cloud Messaging 實作推播與 deep links（促銷及會員里程碑）。

<br />

---

## 其他工作

忠誠度與會員應用、CMS 內容站點、Shopify storefronts、健康與保健 RAG agent，以及 RBAC 資源系統——多為 Next.js／React Native。

<br />

---

## 技能

- **程式語言**：TypeScript、Node.js、SQL
- **前端**：React、Next.js、React Native、Expo、TanStack Query、TanStack Form、XState、TanStack Table、Tailwind CSS、shadcn/ui、Chakra UI、Framer Motion、Storybook、Figma、TipTap、D3
- **後端**：Hono、Drizzle、Prisma、OpenAPI、tRPC、GraphQL、Postgres、DynamoDB、Better Auth
- **Auth 與 i18n**：OAuth 2.0／OIDC、JWT、HttpOnly cookies、CORS、RBAC、next-intl
- **AI**：Mastra、Vercel AI SDK、Langchain、Pinecone、Strands、AWS Bedrock
- **測試與監控**：Cypress、Vitest、React Testing Library、Lighthouse、axe、Sentry、PostHog
- **工作流程**：Cursor、Claude Code
- **DevOps**：SST、Docker、ECS Fargate、GitHub Actions、AWS、Turborepo

<br />

---

## 認證

- AWS Solutions Architect Associate，2026
- AWS Cloud Practitioner，2025

<br />

---

## 教育背景

### University College London

- MPharm 藥學，2016 至 2020 年，一等榮譽學位
- 熱愛動手建構。從藥劑轉行至軟件開發。

<br />

---

你亦可以在[這裡](/resume.pdf)下載我的履歷。
