---
title: 履歷 — 全端
---

## Wilson, Chow Sin Long

擁有 5 年經驗的全端軟件工程師，負責從 Figma、React／React Native、型別安全 API 到 AWS 部署的完整交付——包括從零到一的上線，以及 **15+** 個應用的多年演進。關鍵路徑載入時間最多縮短 **80%**，搜尋延遲降低 **90%**。目前主導一個具備持久化、可中斷 workflows、memory、RAG、scorers 與 evals 的多渠道 agent 平台。日常開發使用 **Cursor**／AI 輔助工具。已取得 **AWS Solutions Architect Associate** 認證。

優勢：端到端交付 · 前端 · 型別安全 API · 生產級 AI agents · AWS

---

## 工作經驗

### 全端工程師，Vioo Company Limited，2022 年 5 月至今

擔任多個並行客戶產品的主要工程師，負責從設計到雲端部署，並與持份者合作規劃 UX 和交付正式版本。

- 從零到一交付並多年維護 **15+** 個正式上線的 React／React Native 應用，包括高保真 Figma 設計與共享設計系統。
- 以自研 headless CMS（Strapi／Sanity）與搜尋（Meilisearch／Algolia）取代第三方 WordPress，過渡期間發布流程不中斷：載入時間縮短 **80%**、搜尋延遲降低 **90%**，PageSpeed Insights **>90**。
- 使用 Mastra、Vercel AI SDK 與 Langchain 交付生產級 AI agents（workflows、memory、tool calls、observability、evals）。
- 建立共享的 Radix／Tailwind 元件與 hook 函式庫，並透過 Storybook、Fumadocs 和 MDX 撰寫文件，供多個客戶應用共用。為關鍵流程加入 Vitest／Cypress。
- 將身分建模為可重用狀態，對接型別安全 API：Better Auth sessions 與組織成員、OAuth 2.0／OIDC、JWT／session 生命週期、RBAC——而非嵌套條件。Session 存放於 **HttpOnly／Secure／SameSite** cookies，而非 `localStorage` tokens。
- 使用 Hono、Drizzle、OpenAPI 與 tRPC 在 AWS Lambda／API Gateway 上建構型別安全 API（Postgres／DynamoDB／S3），並以 Scalar 提供文件。加固邊緣：**嚴格 CORS**、trusted origins、auth 路由的 WAF 限流，身分來自 authorizer cookie——絕不信任客戶端傳入的 user header。API keys、tokens 與憑證存放於 **SST Secret**，而非 `.env`。GitHub Actions + SST 做 CI／CD——部署前跑 lint、typecheck、tests 與 build——部署時間縮短 **50%+**。

<br />

---

## 精選專案

### 多渠道 Agent 平台，2026 年 4 月至今

面向客戶的持久化多渠道助手平台（Web + Discord）。

- 將每次 agent run 建模為**有狀態、可中斷、可恢復**的持久化串流：客戶端可斷線、重連並在執行途中恢復，而不會遺失進度——而非嵌套的聊天條件。
- 基於 Mastra 與 AI SDK 建構 workflow、memory、scheduling 和 workspace 原語：pause／resume、retries、沙盒工具與 skills。
- 端到端交付 RAG（ingest、chunk、Pinecone query／rerank），並以帶評分的 evals（golden prompts + LLM-as-judge）作為發布門控。
- 將 Hono + Better Auth 部署於 ECS，以 Postgres 儲存核心資料、S3 儲存檔案，並以 ClickHouse 記錄 run／tool traces 與成本／延遲。
- 交付可組合的聊天、工具調用與多步客戶端流程，以及 TipTap @mention 檔案標記元件，用於將 workspace 檔案附加至 prompts。

---

### 高效能行銷網站，2025 年 11 月

擔任內容與搜尋型行銷網站的主要工程師——效能、CMS 與發布流程。

- Next.js + Strapi + Meilisearch，達到 **LCP &lt; 0.5s**、**CLS &lt; 0.1**，PageSpeed Insights **>90**。
- 混合 ISR：主要頁面與最新 100 篇文章在建置時生成，其餘按需；透過 Strapi webhook revalidation，發布後無需完整重建即可上線。
- 將無障礙與 SEO 納入交付標準；使用 AI SDK 建構用於生成式 AI 內容創作的自訂 Strapi plugin。
- 以 TanStack Query prefetch／hydration 與 TanStack Form + Zod 編排互動與多步流程；客戶內容站點支援 locale-aware 路由、文案與日期／數字格式。

---

### React Native 會員平台，2024 年 4 月上線

- 主導 Figma 設計與會員應用；將 iOS 和 Android 發布至 App Store 與 Play Store。同時維護 Express API 與 Next.js 管理後台。
- 透過 Sentry 獨立診斷生產崩潰、裝置相容性與效能瓶頸；上線後維持 **>99.5%** crash-free sessions。
- 使用 FlashList、tab freeze 與快取 feed 圖片改善列表卡頓。Firebase Cloud Messaging 實作推播與 deep links（促銷及會員里程碑）。

<br />

---

## 其他工作

忠誠度與會員應用、CMS 內容站點、Shopify storefronts、健康與保健 RAG agent，以及 RBAC 資源系統——多為 Next.js／React Native。

<br />

---

## 技能

- **程式語言**：TypeScript、Node.js
- **前端**：React、Next.js、React Native、Expo、TanStack Query、TanStack Form、Tailwind CSS、shadcn/ui、Storybook、Figma、TipTap
- **後端**：Hono、Drizzle、OpenAPI、tRPC、GraphQL、Postgres、DynamoDB、Better Auth
- **Auth 與 i18n**：OAuth 2.0／OIDC、JWT、HttpOnly cookies、CORS、RBAC、next-intl
- **AI**：Mastra、Vercel AI SDK、Langchain、Pinecone
- **測試與監控**：Cypress、Vitest、React Testing Library、Lighthouse、axe、Sentry、PostHog
- **工作流程**：Cursor、Claude Code
- **DevOps**：SST、SST Secret、Docker、GitHub Actions、AWS、Turborepo

<br />

---

## 認證

- AWS Solutions Architect Associate，2026
- AWS Cloud Practitioner，2025

<br />

---

## 教育背景

### University College London

- MPharm 藥學，2016 至 2020 年，一等榮譽學位
- 註冊藥劑師（GPhC），2021 年至今
- 等效行業經驗：5 年交付 15+ 個正式上線產品；AWS Solutions Architect Associate。藥劑執業屬受規管、高風險領域——習慣在合規約束下作出產品取捨。

<br />

---

你亦可以在[這裡](/resume.pdf)下載我的履歷。
