---
title: 履歷
---

## 概要

擁有 5 年經驗的軟件開發者。負責從 **Figma** 設計、**React** 開發，到後端與雲端部署的全端交付。已推出超過 15 個正式上線的應用。已取得 **AWS Solutions Architect Associate** 認證。

打造過設計系統、進階動畫，以及高效能的 **Next.js**／**React Native** 應用。後端使用 **Hono**、**Drizzle**、**OpenAPI**、**tRPC** 與 **GraphQL**。Headless CMS 與搜尋使用 **Strapi**、**Sanity** 與 **Meilisearch**。

有生產級 **AI agents**、**RAGs** 與 **MCP servers** 經驗，使用 **AI SDK**、**Langchain** 與 **Mastra**。

擅長以 **SST**、**Terraform**、**AWS** 與 **Vercel** 部署並管理雲端基礎設施。

<br />

---

## 工作經驗

### 軟件開發者，Vioo Company Limited，2022 年 5 月至今

- 端到端開發並維護超過 15 個正式上線的 **React**／**React Native** 應用，包括高保真 **Figma** 設計與**設計系統**。
- 與客戶及跨職能團隊合作，釐清需求、迭代 UX，並交付正式版本。
- 建立可共用的元件／hook 函式庫（**Radix**、**Tailwind CSS**），並以 **Storybook**／**Fumadocs**／**MDX** 撰寫文件，供多個客戶專案使用。
- 使用 **Framer Motion** 打造進階動畫與微互動（scroll-based、animate presence、layout transitions），應用於多個客戶產品。
- 使用 **D3** 與 **Framer Motion** 打造進階動畫折線圖。
- 使用 **Mastra**、**AI SDK** 與 **Langchain** 打造生產級 **AI agents**（workflows、memory、workspaces、observability）；並交付可重用的 agent 聊天與 tool invocation UI。
- 以現代 **headless CMS**（**Strapi**／**Sanity**）與搜尋（**Meilisearch**／**Algolia**）取代舊有 **WordPress** 架構。網站載入時間減少 **80%**，內容搜尋延遲減少 **90%**，PageSpeed Insights 分數達 **>90**。
- 使用 Shopify headless **GraphQL** API 打造自訂 **Next.js** storefront。
- 使用 **Hono**、**Drizzle**、**OpenAPI** 與 **tRPC** 在 **AWS Lambda**／**API Gateway** 上建構 typed API，資料層使用 **Postgres** 與 **DynamoDB**，並以 **Scalar** 提供 API 文件。
- 導入 **SST** 管理應用部署與基礎設施，建置與部署時間縮短超過 **50%**。
- 主導前端程式碼與效能審查——推動 composable React patterns、重型 UI 的 lazy loading——並為關鍵流程加入 **Cypress** E2E 覆蓋。

<br />

---

## 精選專案

### _Multi-channel Durable Agent，2026 年 4 月至今_

- 主導 **multi-channel** agent 平台（web + Discord），支援 durable streaming，客戶可在 agent 執行中斷線並於中途重連繼續。
- 使用 **Mastra** 與 **AI SDK** 打造 workflow、memory、scheduling 與 workspace 原語——支援 pause／resume、retries、檔案儲存、沙盒工具與 skills。
- 端到端交付 RAG：ingest／parse、chunking、**Pinecone** retrieval（含 query／rerank，以及需要時的 graph query）。
- 後端為 **Hono** + **Better Auth**，部署於 **ECS**；核心資料使用 **Postgres**，檔案系統使用 **S3**。
- 打造 agent 可觀測性與評估：在 **ClickHouse** 記錄 run／tool traces 與成本／延遲指標，並以 scored eval suites（golden prompts + LLM-as-judge）捕捉品質回退。
- 使用 **TipTap** 設計並交付自訂 **@mention** 檔案標記元件，用於 agent 提示輸入。

---

### _Incremental Static Regeneration Next.js Site，2025 年 11 月_

- 打造 **Next.js** 內容網站，後端為 **Strapi**、搜尋為 **Meilisearch**，達到 **LCP < 0.5s**、**CLS < 0.1**，PageSpeed Insights **>90**。
- 混合式 **ISR**：主要頁面與最新 100 篇文章於建置時預渲染；其餘頁面按需以 fallback 生成。
- 透過 Strapi 內容 webhook 進行 **tag-based revalidation**，發布後可更新線上頁面而無需完整重建。
- 互動 UI 使用 **TanStack Query** 伺服器端 prefetch／hydration；表單以 **TanStack Form**、**Zod** 與 Query mutations 處理。
- **Turborepo** 透過 **SST** 部署至 **AWS ECS**，經 **API Gateway** 路由；媒體存放於 **S3**。
- **無障礙（Accessibility）**：語意化 landmarks／標題、鍵盤可操作的導覽與表單、可見 focus，以及 CMS 媒體的有意義 alt text；以 **Lighthouse** 與 **axe** 進行 a11y 測試。
- **SEO**：來自 Strapi 的 metadata／OG tags、canonical URLs、sitemap + `robots.txt`，以及 Article JSON-LD 結構化資料，提升內容頁可索引性。

---

### _React Native App，2024 年 4 月_

- 主導餐飲品牌會員應用的 **Figma** 設計與 **React Native** 前端（電子會員卡、積分、優惠）。
- 與後端整合來自 **OpenAPI**／Swagger 的 typed REST clients；並上架 iOS 與 Android 至 App Store 與 Play Store。
- 實作推播與 deep-linked 行銷活動，用於促銷與會員里程碑。
- 上線後維持 **>99.5%** crash-free sessions，並以 **Sentry** 監控。
- 以 **FlashList**（recycling、調校 batch／window）取代 ScrollView 映射列，改善選單／歷史列表卡頓。
- 透過 **react-native-screens** freeze 與延遲掛載畫面，降低非作用中分頁成本；以 **Reanimated** 處理原生執行緒轉場。
- 快取動態圖片，並避免在長列表中使用全解析度素材。

<br />

---

## 其他專案

我也負責開發以下專案：

- **健康與保健 RAG agent**（Next.js、Langchain、Shopify 產品檢索）
- **餐廳忠誠度應用**（React Native）
- **商業組織會員入口**（React Native）
- **活動報名網站**（Next.js）
- **活動／行銷網站**（Next.js、Sanity／Strapi）
- **資源管理系統（含 RBAC）**（Next.js）
- **電商 storefront**（Next.js、Shopify GraphQL）
- **居家長者照護應用**（Flutter）
- **圖書訂購系統**（Laravel、React）

---

## 技能

- **程式語言**：Typescript、Python
- **Frontend**：React、Next.js、React Native、Expo、Tanstack Query、Framer Motion、Tailwindcss、Shadcn UI、Storybook、Figma、MDX
- **Backend**：Hono、Drizzle、OpenAPI、tRPC、Scalar、Pinecone、Meilisearch、Strapi、Sanity
- **AI**：Mastra、AI SDK、Langchain、Strands
- **Testing**：Cypress、Vitest、React Testing Library、Lighthouse、axe
- **Database**：Postgres、DynamoDB
- **DevOps**：Docker、SST、AWS、Vercel、Turborepo

<br />

---

## 教育背景

### University College London

- MPharm 藥學，2016 - 2020
- 一等榮譽學位

<br />

---

## 認證

- AWS Solutions Architect Associate，2026
- AWS Cloud Practitioner，2025

<br />

---

你也可以在[這裡](/resume.pdf)下載我的履歷。
